package moe.plushie.rpgeconomy.api.quest;

public enum QuestRewards {
    /** Player receives an item. */
    ITEM_RECEIVE,
    /** Player picks an item to receive. */
    ITEM_RECEIVE_CHOICE,
    /** Player receives a mail message. */
    MAIL_RECEIVE,
    /** Player receives currency. */
    CURRENCY_RECEIVE,
    /** Player receives reputation. */
    REPUTAION_RECEIVE
}
